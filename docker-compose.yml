version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - '8080:8080'
    environment:
      - PORT=8080
  frontend:
    build: ./frontend
    ports:
      - '5173:4173'
    environment:
      - VITE_API_BASE=http://backend:8080
    depends_on:
      - backend
